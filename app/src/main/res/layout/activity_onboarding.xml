<?xml version="1.0" encoding="utf-8"?>
<!--
    activity_onboarding.xml
    Root layout: full-screen gradient bg + ViewPager2 + bottom controls overlay
-->
<FrameLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/gradient_bg_dark_green">

    <!-- Dark overlay for extra depth -->
    <View
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="#33000000" />

    <!-- ViewPager2 — fills the entire screen -->
    <androidx.viewpager2.widget.ViewPager2
        android:id="@+id/viewPager"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <!-- Bottom control overlay: dots + next/skip button -->
    <LinearLayout
        android:id="@+id/bottomControls"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom"
        android:orientation="vertical"
        android:paddingStart="24dp"
        android:paddingEnd="24dp"
        android:paddingBottom="40dp">

        <!-- Dots indicator: frosted glass pill wrapping the individual dots -->
        <FrameLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center_horizontal"
            android:layout_marginBottom="24dp"
            android:background="@drawable/bg_onboarding_dots_pill"
            android:paddingStart="10dp"
            android:paddingEnd="10dp"
            android:paddingTop="7dp"
            android:paddingBottom="7dp">

            <LinearLayout
                android:id="@+id/dotsContainer"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:gravity="center_vertical" />

        </FrameLayout>

        <!-- Next / Get Started button -->
        <FrameLayout
            android:layout_width="match_parent"
            android:layout_height="52dp"
            android:elevation="4dp"
            android:translationZ="2dp"
            android:background="@drawable/bg_button_primary"
            android:id="@+id/btnNext">

            <TextView
                android:id="@+id/tvNextLabel"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="center"
                android:text="NEXT"
                style="@style/TextStyle.ButtonCta" />
        </FrameLayout>

    </LinearLayout>

    <!-- Skip button — top right -->
    <TextView
        android:id="@+id/btnSkip"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="top|end"
        android:layout_marginTop="56dp"
        android:layout_marginEnd="20dp"
        android:text="Skip"
        android:textColor="#CCFFFFFF"
        android:textSize="16sp"
        android:padding="12dp" />

</FrameLayout>
