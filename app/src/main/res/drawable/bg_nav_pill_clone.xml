<?xml version="1.0" encoding="utf-8"?>
<!--
    bg_nav_pill_clone.xml — Bottom Nav Bar Pill (Container)
    iOS reference: a translucent frosted pill that lets the background bleed through.
    Brighter rim + more transparent body.
-->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Glass body — translucent frosted pill -->
    <item>
        <shape android:shape="rectangle">
            <solid android:color="@color/glass_nav" />
            <corners android:radius="100dp" />
        </shape>
    </item>

    <!-- Top specular — thin bright top-edge on the pill -->
    <item android:bottom="0dp">
        <shape android:shape="rectangle">
            <gradient
                android:type="linear"
                android:angle="270"
                android:startColor="@color/glass_specular_mid"
                android:endColor="#00FFFFFF"
                android:centerY="0.3" />
            <corners android:radius="100dp" />
        </shape>
    </item>

    <!-- Rim border -->
    <item>
        <shape android:shape="rectangle">
            <corners android:radius="100dp" />
            <stroke
                android:width="1dp"
                android:color="@color/glass_rim_outer" />
        </shape>
    </item>

</layer-list>
